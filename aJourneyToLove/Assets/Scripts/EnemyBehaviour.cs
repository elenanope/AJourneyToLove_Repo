using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBehaviour : MonoBehaviour
{

    [SerializeField] bool patrollingEnem; //para los enemigos que solo patrullan
    [SerializeField] bool stalkerEnem; //no sé si ponerlo (enemigo que te sigue)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
